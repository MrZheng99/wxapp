<view class="container">
    <block wx:for="{{title}}" wx:key="this">
        <button  class="title" hover-class="hoverclass" bindtap="changeToTxtPage" id="{{index}}">
            {{index+1}}:{{item}}
        </button>
    </block>
      
</view>